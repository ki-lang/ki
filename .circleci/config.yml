version: 2.1

jobs:
  build_macos:
    macos:
      xcode: "12.5.1"
    steps:
      - checkout
      - run:
          command: |
            make

workflows:
  build_macos:
    jobs:
      - build_macos
