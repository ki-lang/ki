
namespace io;

#header "sys/io"

func println(string str) {
	main:main:write(1, str.data, str.length);
	print("\n");
}

func print(string str) {
	main:main:write(1, str.data, str.length);
}

func write(i32 fd, string str) {
	main:main:write(fd, str.data, str.length);
}
