
class ptr type:ptr math async {

	func str() .String {
        let num = this @as uxx;
        return num.str_base(16);
    }

    func debug() void {
        let num = this @as uxx;
        num.debug_base(16);
    }
}
