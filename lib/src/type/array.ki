
class Array[T] {
	readonly size: uxx;
	readonly length: uxx;

	static func init(mut start_size: uxx = 2) .CLASS {
		if start_size < 2 {
			start_size = 2;
		}
		return CLASS {
			size: start_size,
			length: 0,
		};
	}

	func push(item: T) void {
	}
}
