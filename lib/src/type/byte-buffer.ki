
use mem;

class ByteBuffer {

	trait ByteBufferTrait;

	// Internals
	func __before_free() void {
		mem:free(this.data);
	}
}

struct ByteBufferStruct {

	trait ByteBufferTrait;

	// Internals
	func free() void {
		mem:free(this.data);
		mem:free(this @as ptr);
	}
}
