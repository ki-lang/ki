
namespace type;

class cstring {

	public func to_string() string {
		u64 len = 0;
		ptr adr = cast this as ptr;
		while(true){
			u8 ch = getptrv adr as u8;
			if(ch == '\0'){
				break;
			}
			len++;
			adr++;
		}
		return string.make(this, len);
	}

}
