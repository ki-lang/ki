
header "sys" as sys;

use io;

class Connection {
	~fd: i32;
	~open: bool = true;

	fn close() {
		if !this.open : return;
		#if OS == win
        sys.closesocket(this.fd @as sys.SOCKET);
        #else
        io:close(this.fd);
        #end
		this.open = false;
	}
}
