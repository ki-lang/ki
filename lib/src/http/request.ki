
class Request {
	readonly http_con: *Connection;
	headers: *Map[String];
	method: *String;
	path: *String;

	func body() String {
		let con = this.http_con;
		let buf = con.read_buf;
		let pos = con.parse_pos;
		return buf.sub_str(pos, buf.length - pos);
	}
}