
namespace http;

#header "sys/net"
#header "sys/io"

use type {PtrArray};

func response_handler(Request req) Response {
	
	return Response {
		body: "Hello",
	};
}

class Server {
	u32 port;
	i32 max_connections = 64000;
	ki:net:Socket socket;
	funcref (Request) Response handler = response_handler;

	func start() ! {

		@ sock = ki:net:Socket { port: this.port };
		sock.listen();

		// 
		@ clientaddr = main:main:sockaddr_in {
			sin_addr: 0
		};

		//u32 addrlen = sizeof(clientaddr);
		u32 addrlen = 16;

		//
		io:print("Running http server on port ");
		io:print(this.port.to_string());
		io:print("\n");

		@ task_list = mem:alloc(this.max_connections * 8);
		@ read_set = mem:calloc(this.max_connections); // todo : too much memory, divide by 8

		fd:add_to_set(read_set, sock.listenfd);

		//
		i32 new_fd = 0;
		while(true){
			i32 result = main:main:select(sock.listenfd + 1, getptr read_set, null, null, null);
			if(result > 0){

			}
		}
	}

	func accept(i32 clientfd){

		@ alc = allocator 48;
		//@ clientaddr = main:main:sockaddr_in {
		//	sin_addr: 0
		//};
		@ clientaddr = alc.get_chunk();
		i32 addrlen = 16;

        i32 new_fd = main:main:accept4(clientfd, clientaddr, getptr addrlen, 0);

		// SOL_SOCKET: 1
		// SO_KEEPALIVE: 9
		i32 keep_alive = 1;
		i32 success = main:main:setsockopt(new_fd, 1, 9, getptr keep_alive, sizeof(i32));

		// O_NONBLOCK
		//i32 flags = main:main:fcntl(new_fd, 3, 0);
		//i32 x = main:main:fcntl(new_fd, 4, flags | 04000);

		alc.free(clientaddr);

		i32 pow = new_fd;
		u64 read_set = 1;
		while(pow > 0){
			read_set *= 2;
			pow--;
		}

		if (new_fd != -1){
			while(true){
				i32 result = main:main:select(new_fd + 1, getptr read_set, null, null, null);
				if(result > 0){
					this.respond(new_fd);
				}
			}
		}else{
			io:print("new_fd failed\n");
		}
	}

	func respond(i32 clientfd){

		@ req = Request.read_request(clientfd);

		@ response = this.handler(req);

		string output = response.output();
		io:write(clientfd, output);

		//i32 result = main:main:shutdown(clientfd, 2); // SHUT_RD=0, SHUT_WR=1, SHUT_RDWR=2
		//i32 result = main:main:close(clientfd);
	}

}
